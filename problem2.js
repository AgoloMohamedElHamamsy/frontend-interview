/**
  implement array.map, array.filter, array.reduce functions with the same exact output.
  [1,2,3].map(i => i+1) -> [2,3,4]
  [1,2,3].filter(i => i<=2) -> [3]
  [1,2,3].reduce((accumulator, currentValue) => accumulator + currentValue) -> 10
**/
function map(arr, cb) {}

function filter(arr, cb) {}

function reduce(arr, cb) {}
